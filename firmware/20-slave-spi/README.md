# 20-slave-spi — Phase 4 (Reserved)

Reserved for Phase 4: ESP-IDF `spi_slave` task pinned to Core 0, DMA blit pipeline to ST7796 on Core 1, shared framebuffer, and the host-side framed RGB565 protocol.

Implementation begins after Phase 3 exit criteria are met. PSRAM availability and the slave wire protocol (`docs/slave_protocol.md`) must be resolved before work starts — see Risks section in [/PLAN.md](../../PLAN.md).
